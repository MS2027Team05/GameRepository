# AI用定義

# 概要
ゲーム制作であることと、その前提を軽く記載。


# 開発環境
Unity 6000.3.24.f1
## 使用エディター(予定)
- VisualStudio
- VisualStudioCode

# 使用しているライブラリ(Package)
- DoTween(v1.2.825)
- Netcode for GameObjects(v2.13.3)
- Multiplayer Tools(2.2.12)
- Multiplayer Play Mode(2.0.2)
- Cinemachine(3.1.7)
- ShaderGraph(17.3.0)
- InputSystem(1.20.0)

# 注意事項
- 回答及びコメントアウトは日本語で実装すること。
- 作業時は`.agents/skills/`配下の各スキル仕様を参照して作業すること。
- ドキュメントやコードのコメント内及び回答の出力にて、主観的で過度な期待を持たせる表現（例：「必ず勝てる」「絶対的な」など）は使用せず、客観的で論理的な表現に留めること。
- 会話は一貫性をもって発言し、コロコロと意見を変えないこと。もし意見を変える場合はその理由を明確に記載すること。
- 全レスポンスに通し番号を振ること。具体的なルールは下記に記す。
  - 形式は`<作業区分No(2桁)>.<実装No(2桁)>.<枝番号(2桁)>`で表記すること。会話をまたいでも番号はリセットせず数えること。
  - プログラムとして`コードを書く作業`や、`ユーザーがEditor上で操作する物`は`<作業区分No(2桁)>`の前に`T`をつけること。そうでないコードの説明等は`<作業区分No(2桁)>`の前に`#`をつけること。`例:#01.01.01`
  - `<作業区分No(2桁)>`は作業の塊を表す。ユーザーから`〇〇な機能と△△な機能を実装してください`や`□□を実装した後に✕✕を実装してください`と言われた際の区切りとしてNoを振ること。
  - 異なるファイルを変更する場合は`<実装No(2桁)>`を変更すること。
  - 該当ファイル内で複数の処理を実装する場合は`<枝番号(2桁)>`を変更すること。`例: T01.01.01 FuncA()の実装, T01.01.02 FuncB()の実装`
- 実装に際して、コードを実装する前に実装計画を作成し、ユーザー側に提案すること。ユーザー側の承認があり次第、次の実装に移行すること。その際、なぜその手段を用いるかについての説明も記載すること。
- 実装が完了した際は、どの部分の実装を行い、どのように実装したか、構造が変化したかの概要を出力すること。
- コード構造は`STRUCTURE.md`に記載している。新規実装によって構造に変化がある場合は追記すること。ファイル名から処理が自明な物は記載せず、各階層構造の責務と重要なエントリーポイントのみを記載すること。
- `AGENTS.md`を更新する場合は、既存の文章を修正しないこと。修正が必要な場合はユーザーの確認を取ること。
- 実装において、判断が難しい箇所がある場合は遠慮なくユーザー側に確認を取ること。
- 思考や判断のループに陥った場合は途中で実行を止め、`どこまで実装できたか`、`どこが実装できなかったのか`、`何が原因で実装できなかったのか`をユーザー側に出力すること。
- 階層、`Assets/Texture/**`、`Assets/Models/**`、`Assets/Audios/**`はAIの学習対象外に含めること。
- Web検索・探索およびブラウザ自動検証の制限について
  - エージェントによるWeb検索やブラウザ探索は、情報の過度な探索による時間消費や不要な巡回を防ぐため、最小限に留めること。
  - 深い調査や詳細情報の確認が必要な場合は、自ら深く探索し続けるのではなく調査対象の概要を提示した上で、詳細な確認作業はユーザー側に委ねるアプローチを取ること。

# ゲームの企画概要
ゲームの企画書が出来てからちゃんと記載かな。
概要としては3D空間上を飛び回り相手のゴールにボールを入れる2vs2のゲーム。
移動方法としてはグラヴィティーデイズのような形式を想定している。前者と操作が異なる部分として、向いている方向に進み続けるのではなくキーを押して移動方向を決定し、その方向に進み続けるというものだ。この場合は周囲を見るためにカメラを動かしてもその方向に進むということはなく周りを見ることが出来る。移動方向を変更したい場合は再度そのキーを押すことで変えられる。
ボール自体はチームメイトと受け渡し(パス)することが出来るが、ボールを保有している人は移動速度が減少する。ボールを相手から奪う場合は現状タックルを想定。

# コーディング規則
- 原則的に`.editorconfig`に従うこと。それ以外のものを下記に記す。
- コメントアウト部分において、全角丸括弧`（）`は使用せず半角丸括弧`()`を使用すること。
## unityでの規約
- uGUI(canvas)を使用し、テキストはTextMeshProを使用すること。
- 外部ライブラリを導入する場合は必ずユーザーに確認すること。
- [Serializeable]が付与されていてInspectorから確認できる変数は、`.cs`側に値を書くのではなく、ユーザーがEditor上で値をセットすること。`.cs`には値を記載しない。
- `GetComponent<T>()`や、`GameObject.Find()`を安易に`Update()`内で呼び出さないこと。`Awake()`や`Start()`、Inspectorからの紐づけでなるべく対応する。
- 大量に同様のオブジェクトを頻繁に生成/削除するのではなく、オブジェクトプール等で使い回すことを検討すること。
- 毎フレームnewでオブジェクトを生成せず、フィールドとして値を持ち、`Clear()`して再利用すること。
- 文字列結合において、高頻度で実行されるものやループ内で実行されるものは、`+`で結合するのではなくStringBuilderを用いて結合すること。
- `List<T>`の要素数が多い場合は探索コストが(O(n))になるため、`Dictionary`や`HashSet`を用いて探索コストを(1(n))にすること。
- 単に距離の大小を比較する場合はsqrt(distance)ではなくsqrMagnitudeを用いること。
- データ・イベント・設定値の管理にはScriptableObjectを使うこと。ただし汎用的な処理や参照箇所が多くなるものに限る。
- クリック判定が不要なUI要素はRaycastTargetをオフにすること。ユーザー側にObjectを作成して貰う場合は該当するObjectの作成時にその旨を記載すること。
- 入力検知にはInputSystemを使うこと。デバッグ時はキーボード+マウスをメインに想定しているが、最終的にゲームをプレイする際はゲームパッドでのプレイを想定している。